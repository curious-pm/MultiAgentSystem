# Core Libraries
crewai        # Framework for managing agents and tasks
whisper           # Speech-to-text transcription
yt-dlp       # Downloads YouTube videos/podcasts
beautifulsoup4   # Web scraping for website analysis
requests        # HTTP requests for fetching website content
PyYAML            # Parsing YAML configuration files (agents.yml)
python-dotenv    # Managing environment variables

# Alias for BeautifulSoup (optional in some setups)
bs4

# Numerical and Computational Libraries
numpy     # Required by Whisper and other libraries

# Additional System-Level Dependency Notes
ffmpeg                   # System dependency required for yt-dlp (not a Python package)
